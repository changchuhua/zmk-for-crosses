if SHIELD_CROSSES_RIGHT

config ZMK_KEYBOARD_NAME
	default "Crosses"

config ZMK_SPLIT_BLE_ROLE_CENTRAL
	default y

endif

if SHIELD_CROSSES_LEFT || SHIELD_CROSSES_RIGHT

if SHIELD_CROSSES_LEFT

config ZMK_KEYBOARD_NAME
    default "Crosses Left"

endif

if SHIELD_CROSSES_RIGHT

config ZMK_KEYBOARD_NAME
    default "Crosses Right"

endif

config ZMK_SPLIT
	default y

endif

